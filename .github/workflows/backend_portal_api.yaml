name: cicd_backend_portal_api

on:
  push:
    branches:
      - main
    paths:
      - 'backend/portal-api/**'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/portal-api/**'

permissions:
  id-token: write
  contents: read

env:
  REGION: europe-west1
  SERVICIO: portal-api
  REPO: repo-recomendador-eventos

jobs:
  ci:
    name: Test build imagen
    runs-on: ubuntu-latest

    steps:
      - name: Cargar repo
        uses: actions/checkout@v4

      - name: Test build imagen
        run: docker build --check backend/${{ env.SERVICIO }}

  cd:
    if: github.ref == 'refs/heads/main'
    name: Build, push y despliegue
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - name: Cargar repo
        uses: actions/checkout@v4

      - name: Autenticacion GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.BACKEND_PORTAL_API_CUENTA_SERVICIO }}

      - name: Configurar Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configurar Docker para Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build y push imagen
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/${{ env.REPO }}/${{ env.SERVICIO }}"
          docker build -t "${IMAGE}:latest" backend/${{ env.SERVICIO }}/
          docker push "${IMAGE}:latest"

      - name: Desplegar en Cloud Run
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/${{ env.REPO }}/${{ env.SERVICIO }}"
          gcloud run services update ${{ env.SERVICIO }} \
            --image="${IMAGE}:latest" \
            --region=${{ env.REGION }} \
            --project=${{ secrets.GCP_PROJECT_ID }}

      - name: Subir public-config.json
        env:
          BUCKET: ${{ secrets.BUCKET_FRONTEND_USUARIOS }}
        run: |
          BACKEND_URL=$(gcloud run services describe portal-api \
            --region=europe-west1 \
            --project="${{ secrets.GCP_PROJECT_ID }}" \
            --format='value(status.url)')
          echo "{\"backendUrl\": \"$BACKEND_URL\"}" > public-config.json
          gsutil -h "Cache-Control:no-cache" cp public-config.json gs://$BUCKET/public-config.json